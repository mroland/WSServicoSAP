/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package br.com.atarde.wsservicosap.business;

import br.com.atarde.wsservicosap.dao.CSTRadioDAO;
import br.com.atarde.wsservicosap.dao.CSTDAO;
import br.com.atarde.wsservicosap.model.CSTModel;
import br.com.atarde.wsservicosap.model.OrigemModel;
import br.com.atarde.wsservicosap.util.Constantes;
import br.com.topsys.util.TSUtil;
import br.com.topsys.web.util.TSFacesUtil;

/**
 *
 * @author mroland
 */
public class CSTBusiness {

    private OrigemModel origemModel;

    public CSTBusiness() {

        this.origemModel =Constantes.ORIGEM;

    }

    public CSTModel obterPeloCodigo(CSTModel model) {

        if (!TSUtil.isEmpty(this.origemModel)) {

            if (Constantes.ORIGEM_RADIO.equals(this.origemModel.getId())) {

                return new CSTRadioDAO().obterPeloCodigo(model);

            }

        }

        return new CSTDAO().obterPeloCodigo(model);

    }
}
