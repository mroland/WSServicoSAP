/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package br.com.atarde.wsservicosap.business;

import br.com.atarde.wsservicosap.dao.ItemRadioDAO;
import br.com.atarde.wsservicosap.dao.ItemDAO;
import br.com.atarde.wsservicosap.model.ItemModel;
import br.com.atarde.wsservicosap.model.OrigemModel;
import br.com.atarde.wsservicosap.util.Constantes;
import br.com.topsys.util.TSUtil;
import br.com.topsys.web.util.TSFacesUtil;

/**
 *
 * @author mroland
 */
public class ItemBusiness {

    private OrigemModel origemModel;

    public ItemBusiness() {

        this.origemModel = Constantes.ORIGEM;

    }

    public ItemModel obter(ItemModel model) {

        if (!TSUtil.isEmpty(model)) {
            
            if (Constantes.ORIGEM_RADIO.equals(this.origemModel.getId())){
                
                return new ItemRadioDAO().obter(model);
                
            }
        }

        return new ItemDAO().obter(model);

    }
}
